extends ../../../shared/views/layout

block title
  | Dashboard - Quad Racing Event Manager

block content
  h2 Quad Racing Event Manager

  p= `Welcome ${pilot.full_name()}`

  !=inspect(current_user, "Current User")
  !=inspect(pilot, "Pilot")
